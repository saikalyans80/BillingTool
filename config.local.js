/**
 * InvoiceFlow — Supabase credentials (loaded before invoicing-tool.html inline defaults).
 * Replace the placeholders below with values from: Supabase Dashboard → Project Settings → API
 * (Project URL + anon public key). Then save and refresh the page.
 */
window.INVOICEFLOW_SUPABASE_URL = 'YOUR_SUPABASE_URL';
window.INVOICEFLOW_SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
window.INVOICEFLOW_ADMIN_EMAIL = 'admin@yourcompany.com';
